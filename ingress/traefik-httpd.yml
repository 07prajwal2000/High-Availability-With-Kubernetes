apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-httpd
spec:
  rules:
  -  http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: httpd-service
            port:
              number: 8080